router nos ayuda a redireccionar sin estar que meter el url del archivo

par empezar ir a los directorios debemos de ir a web.php

web nos ayudara a ir a las rutas para donde queramos ir

Route::get"funcoin que tiene parametros ('/' "la ruta de que tipo get" , function () {
    return view('welcome');
});

Esta parte representa una segunda funcion 
function () {
    return view('welcome');
});
